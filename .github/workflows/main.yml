name: CI on Commit
on: [push] # Triggers on every push to any branch

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run a script
        run: pwd && ls && whoami
